groups:
  - name: api_alerts
    rules:
      - alert: HighErrorRate
        expr: vector(1) > 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Taux d'erreur élevé détecté"
          description: "Alerte test - toujours vraie"